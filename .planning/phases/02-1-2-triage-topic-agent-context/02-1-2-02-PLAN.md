---
phase: 02-1-2-triage-topic-agent-context
plan: 02
type: execute
wave: 2
depends_on: [02-1-2-01]
files_modified:
  - ../openclaw-ops/vps/compose/.env.example
  - ../openclaw-ops/vps/README.md
autonomous: true
gap_closure: true
requirements:
  - TRIAGE-02.1.2-2
must_haves:
  truths:
    - "Inside the production OpenClaw container, ByteRover `brv` provider connect succeeds non-interactively using secrets from compose/env (no committed tokens)."
    - "Smoke retrieval or `memory_search` parity check passes from same cwd/workspace Bella uses (/data/.openclaw/workspace)."
    - "`brv providers connect byterover` no longer returns auth-required during normal agent turns."
---

<objective>
Align ByteRover / `brv` authentication with the runtime Bella actually uses on the VPS: same container (`openclaw-ridl-openclaw-1`), same workspace path agents use.

Purpose: Secondary memory augmentation works; Bella stops wasting turns on failing `exec`/connect loops when Plan 02-1-2-01 already supplies digest markdown.
Output: Updated ops compose template + README runbook notes + verified `brv providers connect byterover` success from container shell.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-1-2-triage-topic-agent-context/02-1-2-CONTEXT.md
@../openclaw-ops/vps/compose/.env.example
@../openclaw-ops/vps/compose/docker-compose.yml
.cursor/rules/vps-ssh-openclaw.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory current brv placement and auth gap</name>
  <read_first>- ../openclaw-ops/vps/compose/docker-compose.yml</read_first>
  <files>../openclaw-ops/vps/compose/docker-compose.yml</files>
  <action>Identify where HOME, volume mounts, and PATH include `/data/.brv-cli/bin/brv`. Confirm which env vars ByteRover docs expect (`BTR_*`, tokens, OAuth). Confirm live failure mode matches prior session log (“ByteRover Provider requires authentication”).</action>
  <acceptance_criteria>In SUMMARY, paste redacted snippet of compose env wiring plan + rationale.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire non-interactive auth into compose template</name>
  <read_first>- ../openclaw-ops/vps/compose/.env.example</read_first>
  <files>../openclaw-ops/vps/compose/.env.example, ../openclaw-ops/vps/README.md</files>
  <action>Add placeholder entries for whichever BYTEROVER / BRV secrets are required — **never real values.** Document rotate + deploy steps. Ensure docker-compose passes vars into container environment if ByteRover reads them out-of-band from user’s auth fix elsewhere, document equivalently (“mount token file bind path X”). Prefer pattern already used elsewhere in compose file.</action>
</task>

<task type="auto">
  <name>Task 3: Verify on live VPS container</name>
  <read_first>- ../openclaw-ops/vps/deploy.sh</read_first>
  <action>SSH from Mac (`ssh openclaw-vps "docker exec …"`) running `cd /data/.openclaw/workspace && /data/.brv-cli/bin/brv providers connect byterover` — MUST exit success. Optionally run simplest memory query/smoke endorsed by ops docs.</action>
  <verify>
    <automated>ssh openclaw-vps "docker exec openclaw-ridl-openclaw-1 … connect byterover"</automated>
  </verify>
  <acceptance_criteria>No “authentication required”; session log no longer repeats brv-auth failure loop.</acceptance_criteria>
</task>

</tasks>

<verification>
- `./deploy.sh` from openclaw-ops when compose env changes mandate it
</verification>

<success_criteria>

- ByteRover provider connect succeeds inside container workspace.
  </success_criteria>

<output>
After completion create `.planning/phases/02-1-2-triage-topic-agent-context/02-1-2-02-SUMMARY.md`.
</output>
