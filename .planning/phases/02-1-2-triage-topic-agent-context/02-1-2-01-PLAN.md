---
phase: 02-1-2-triage-topic-agent-context
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/telegram/bot-message-context.session.ts
  - src/telegram/bot-message-context.ts
  - src/telegram/triage-topic-digest-snapshot.ts
  - docs/agent-architecture.txt
autonomous: true
gap_closure: true
requirements:
  - TRIAGE-02.1.2-1
  - TRIAGE-02.1.2-3
must_haves:
  truths:
    - "Inbound Telegram Forum turns in the configured triage topic thread inject canonical digest markdown into the agent context (bounded), so Bella sees the same markdown the digest script wrote — not merely prior chat bubbles."
    - "When the digest file is missing or empty, Bella receives an explicit sentinel line (never silent failure)."
    - "Core Telegram changes ship through openclaw-ops VPS patch/deploy path when building for production container."
  artifacts:
    - path: "src/telegram/bot-message-context.session.ts"
      provides: "Hook that appends bounded digest preamble or GroupSystemPrompt segment when forum thread matches triage topic binding."
    - path: "docs/agent-architecture.txt"
      provides: "Documents triage-topic digest injection contract and sizing/cap semantics."
---

<objective>
Make Plan C workable end-to-end: when the operator speaks in the **Triage Digest** Telegram Forum topic, Bella’s inbound agent envelope includes the canonical `triage-digest-latest.md` content — not hypothetical thread history she cannot see — so verbs like draft / summary / ignore operate on grounded items.

Purpose: Eliminate reliance on flaky chat transcript visibility and on optional memory backends for baseline triage cognition.
Output: Implemented + tested Telegram wiring upstream; VPS deploy path documented; live repeat of UAT 1–3 without paste.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-1-2-triage-topic-agent-context/02-1-2-CONTEXT.md
@src/telegram/bot-message-context.session.ts
@src/telegram/bot-message-context.ts
@.cursor/rules/vps-ssh-openclaw.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover binding surface for Forum topic ↔ triage thread</name>
  <read_first>
    - src/telegram/bot-message-context.ts
    - src/telegram/bot-message-context.session.ts
  </read_first>
  <files>src/telegram/bot-message-context.ts, src/telegram/bot-message-context.session.ts</files>
  <action>Locate where `topicConfig`, `resolvedThreadId`, `message_thread_id`, and `finalizeInboundContext` converge. Decide the smallest extension: new optional field on Telegram topic config OR env-gated `(chatId, threadId)` match for injecting digest — prefer config-driven mapping that works with existing `telegram.groups.*.topics` schema. Confirm triage digest topic numeric id matches production (reuse existing docs / env from cron scripts).</action>
  <verify>
    <automated>pnpm exec vitest run src/telegram --passWithNoTests 2>/dev/null | tail -20</automated>
  </verify>
  <acceptance_criteria>A written decision in SUMMARY.md: injection trigger is explicit (forum + thread id + optional label), not heuristic.</acceptance_criteria>
  <done>Injection predicate is deterministic and mirrors how digest is posted to Telegram.</done>
</task>

<task type="auto">
  <name>Task 2: Implement digest markdown read + bounded inject</name>
  <read_first>
    - src/telegram/bot-message-context.session.ts
  </read_first>
  <files>
    src/telegram/triage-topic-digest-snapshot.ts,
    src/telegram/bot-message-context.session.ts
  </files>
  <action>Add a small helper module (preferred) reading digest path default `/data/openclaw-gws/output/triage-digest-latest.md` with overrides via env `OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_PATH` (or equivalent config key documented in PLAN SUMMARY cap ~32k chars with tail sentinel). Concatenate digest into inbound context labeled clearly (`## Canonical triage digest snapshot (not user message)` ...) before user body. Respect UTF-8. No shelling out unless already standard in codebase — prefer fs read with Node existsSync guard.</action>
  <verify>
    <automated>pnpm test -- src/telegram 2>/dev/null | tail -30</automated>
  </verify>
  <acceptance_criteria>Unit/integration tests cover inject-on-match, skip-on-non-match, truncate-on-oversized. No secret paths in tests — use fixtures.</acceptance_criteria>
  <done>Agent receives digest snapshot deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Docs + VPS patch path note</name>
  <read_first>- docs/agent-architecture.txt</read_first>
  <files>docs/agent-architecture.txt, ../openclaw-ops/vps/README.md</files>
  <action>Extend `agent-architecture.txt` triage section with triage-topic digest injection. Add short pointer in ops README or patch readme that VPS deploy for core Telegram changes flows through `./vps/build-patched-openclaw.sh` after patch update (no ad-hoc container edits).</action>
  <verify>
    <automated>rg \"triage digest\" docs/agent-architecture.txt</automated>
  </verify>
  <acceptance_criteria>Architecture doc mentions injection field and sizing cap rationale.</acceptance_criteria>
  <done>Operators know what Bella sees vs. what humans see.</done>
</task>

</tasks>

<verification>
- `pnpm build`
- `pnpm test -- src/telegram/**/*.test.ts` (or project's nearest pattern)
</verification>

<success_criteria>

- Triage-topic inbound envelopes include bounded digest markdown per TRIAGE-02.1.2-1.
- Tests prevent regression (missing file, oversized file, wrong topic).
  </success_criteria>

<output>
After completion create `.planning/phases/02-1-2-triage-topic-agent-context/02-1-2-01-SUMMARY.md`.
</output>
