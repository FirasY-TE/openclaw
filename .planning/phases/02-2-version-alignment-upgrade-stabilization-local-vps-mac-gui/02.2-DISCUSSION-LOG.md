# Phase 02.2: Version Alignment + Upgrade Stabilization — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.  
> Decisions are captured in `02.2-CONTEXT.md`.

**Date:** 2026-05-03  
**Phase:** 02.2 — Version alignment + upgrade stabilization (local + VPS + Mac GUI)  
**Areas discussed:** Target pinning, Upgrade sequence + checkpoints, Smoke depth, Mac GUI vs VPS coupling

---

## Selection step

**User choice:** Selected **all** gray areas offered (multi-select: entire list).

**Follow-up:** Recommended defaults applied per area below (no `--auto` flag on command; user did not request `--auto`, only “all” for area coverage).

---

## 1. Target pinning

| Option | Description                                                                            | Selected |
| ------ | -------------------------------------------------------------------------------------- | -------- |
| A      | Single documented triple: semver + upstream SHA + patch identity; local matches semver | ✓        |
| B      | Same release family only; SHA documented only on VPS                                   |          |
| C      | Track latest stable on each surface independently                                      |          |

**User's choice:** **A** (recommended default for area 1)  
**Notes:** VPS truth is **pinned SHA + patches**; local tracks **documented semver**; tarball hash/name recorded post-build.

---

## 2. Upgrade sequence + rollback checkpoints

| Option | Description                              | Selected |
| ------ | ---------------------------------------- | -------- |
| A      | Local → VPS → Mac; checkpoint after each | ✓        |
| B      | VPS → local → Mac                        |          |
| C      | Parallel upgrades                        |          |

**User's choice:** **A** (recommended default)  
**Notes:** Rollback documented as reinstall prior tarball / npm / app build — bullet checklist in plan/UAT.

---

## 3. Smoke depth

| Option | Description                                                         | Selected |
| ------ | ------------------------------------------------------------------- | -------- |
| A      | ROADMAP UAT + minimal grep/probe checklist (scripted or copy-paste) | ✓        |
| B      | UAT manual only                                                     |          |
| C      | Extended automated suite beyond phase need                          |          |

**User's choice:** **A** (recommended default)  
**Notes:** Keeps phase bounded while making repeats deterministic.

---

## 4. Mac GUI vs VPS coupling

| Option | Description                                                               | Selected |
| ------ | ------------------------------------------------------------------------- | -------- |
| A      | Same release family; patched VPS may diverge from GUI npm; document drift | ✓        |
| B      | Strict byte-match between GUI and container package                       |          |
| C      | GUI optional for phase gate                                               |          |

**User's choice:** **A** (recommended default)  
**Notes:** Sparkle cadence may lag; gate on probes + triage UAT, not identical package bytes.

---

## Claude's Discretion

- RUNBOOK placement and whether smoke is one script vs documented one-liners.

## Deferred Ideas

- None recorded.
