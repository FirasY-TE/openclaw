# Phase 02.2 — Research (operator / deploy alignment)

## RESEARCH COMPLETE

**Scope:** Version alignment across **local CLI**, **VPS patched tarball** (`openclaw-ops`), and **Mac GUI gateway**. No greenfield application code in `openclaw/openclaw` for this phase unless a doc fix is needed.

## Findings

### Surfaces and source of truth

| Surface     | Source of truth                                                                                                        | Version check                                             |
| ----------- | ---------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| VPS runtime | `~/openclaw-ops/vps/.openclaw-version` + `vps/patches/*.patch` → `build-patched-openclaw.sh` → `deploy.sh --with-core` | `docker exec openclaw-ridl-openclaw-1 openclaw --version` |
| Local CLI   | Operator install channel (typically global `npm i -g openclaw@<semver>`)                                               | `openclaw --version`                                      |
| Mac GUI     | Sparkle/stable app build                                                                                               | About box / app info; gateway restart via app             |

Per **02.2-CONTEXT.md D-09/D-10**, VPS npm tarball may differ byte-wise from Mac GUI embedded runtime; **release family + probes + triage UAT** gate acceptance.

### Upgrade order (locked)

**Local → VPS → Mac** (D-04). Each step gets a **checkpoint** before the next (D-05).

### Retired artifacts (must stay absent)

ROADMAP success criteria and **02.1 CONTEXT** refer to **Plan C decommission**, not the **02.1.3 digest-snapshot patch**:

- **Forbidden:** `OPENCLAW_TRIAGE_DRAFT_*` env vars, `openclaw-gws-triage-draft-agent`, old auto-route / draft-agent patched core workflow.
- **Expected on VPS after 02.1.3:** `openclaw-ops` patched tarball that includes **triage digest snapshot** behavior (`build-patched-openclaw.sh`); verify via grep for snapshot symbols **inside container** if needed (`readTriageDigestSnapshot`, `OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_*` per 02.1.3 docs).

### Rollback

**Version-only rollback:** prior `.openclaw-version` + prior patch set → rebuild tarball → `deploy.sh --with-core`; local `npm install -g openclaw@<prior>`; reinstall prior Mac app build. Document exact commands in phase VERIFICATION.

### Requirement IDs (machine traceability)

| ID                | ROADMAP requirement (paraphrase) | Plans          |
| ----------------- | -------------------------------- | -------------- |
| ALN-02.2-LOCAL    | Upgrade local CLI + probes       | 02             |
| ALN-02.2-VPS      | Upgrade VPS via openclaw-ops     | 03             |
| ALN-02.2-MAC      | Mac GUI + gateway restart        | 04             |
| ALN-02.2-SNAPSHOT | Pre/post snapshots all surfaces  | 01, 02, 03, 04 |
| ALN-02.2-SMOKE    | Triage smoke + 02.1 checks       | 04             |
| ALN-02.2-ROLLBACK | Rollback RUNBOOK + checkpoints   | 01             |

- Compose recreate or global npm upgrade can **silently drop** a patched install — checkpoints capture tarball identity to detect drift.
- `channels status --probe` is the **fast health gate** after each surface upgrade.

---

## Validation Architecture

**Dimension 8 (Nyquist):** This phase is predominantly **operator-driven verification**. Automated coverage in `openclaw` repo is limited to **documentation artifacts** and **grep-able checklist strings** in `.planning/phases/02-2-*/`.

| Dimension       | Strategy                                                                                           |
| --------------- | -------------------------------------------------------------------------------------------------- |
| Correctness     | ROADMAP UAT bullets + CONTEXT D-\* gates                                                           |
| Observability   | Pre/post version snapshots in `02.2-VERSION-TARGET.md`                                             |
| Failure modes   | Rollback RUNBOOK in `02.2-VERIFICATION.md`                                                         |
| Performance     | N/A (ops phase)                                                                                    |
| Security        | No new secrets; confirm `.env` not committed                                                       |
| Accessibility   | N/A                                                                                                |
| Maintainability | Single target doc + VERIFICATION as repeat playbook                                                |
| Testability     | `grep`/`docker exec`/`openclaw channels status --probe` commands copied verbatim into VERIFICATION |

**Wave 0:** Not required — no new unit test harness; verification is CLI + manual triage path.
