# Phase 02.2 — Version alignment snapshot

**Target release family (D-02):** Align local CLI/runtime, VPS patched container, and Mac GUI app on the intended OpenClaw release line for this rollout.

**Documented target semver (npm / global CLI):** **`2026.5.2`** (npm dist-tag / package version family for this rollout).

**Upstream pin (`~/openclaw-ops/vps/.openclaw-version`):** **`8b2a6e57fef6c582ec6d27b85150616f9e3a7ba4`** (short **`8b2a6e5`**) — upstream tag **`v2026.5.2`**.

**Patch set:** `~/openclaw-ops/vps/patches/` (`0001` digest snapshot, `0002` triage CLI + Telegram `/triage`) applied with `build-patched-openclaw.sh`.

**Note (D-01 / D-09 / D-10):** The VPS row reflects the **patched** global `openclaw` on **`PATH`** inside `openclaw-ridl-openclaw-1` (typically **`/data/.npm-global/bin/openclaw`**), not the stock tree under `/usr/local`. Local npm and Mac Sparkle may trail the VPS pin—record drift explicitly below.

---

## Pre-upgrade (checkpoint 0)

| Label                                             | Value                                                                                                    |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| Local openclaw --version                          | _(prior)_ OpenClaw 2026.4.12 (1c0672b) — see pre-02.2 history in git if needed                           |
| VPS docker exec … `which openclaw && openclaw -v` | _(prior)_ aligned to 2026.4.12 family before 2026.5.2 ops bump                                           |
| Mac GUI version/build                             | OpenClaw Mac `2026.4.11` (CFBundleVersion `2026041190`) — documented drift vs npm/VPS                    |
| openclaw-ops vps/.openclaw-version (upstream SHA) | `1c0672b74f66038fd4ee76fbf1c21715887149d8` → bumped to `8b2a6e57fef6c582ec6d27b85150616f9e3a7ba4` in ops |
| Tarball path + SHA256                             | Operator: record from `build-patched-openclaw.sh` output at rebuild time                                 |

---

## Post-upgrade (checkpoint 3) — **2026-05-04 closure**

| Label                                             | Value                                                                                                                                     |
| ------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Local openclaw --version                          | _(optional follow-up)_ align to `2026.5.2` when convenient — not blocking triage UAT                                                      |
| VPS docker exec … `which openclaw && openclaw -v` | **OpenClaw 2026.5.2 (8b2a6e5)** — `/data/.npm-global/bin/openclaw` (see `openclaw-ops/vps/README.md` “OpenClaw CLI inside the container”) |
| Mac GUI version/build                             | _(unchanged unless operator bumped Sparkle)_ — D-09/D-10 allows Mac to trail VPS at patch granularity                                     |
| openclaw-ops vps/.openclaw-version (upstream SHA) | **`8b2a6e57fef6c582ec6d27b85150616f9e3a7ba4`** — committed **`openclaw-ops`** `92cbbe5`                                                   |
| Tarball path + SHA256                             | Rebuild via `./build-patched-openclaw.sh` when bumping pin; deploy with `OPENCLAW_PACK_TGZ=… ./deploy.sh --with-core --restart`           |

---

## Closure note (operator)

Phase 02.2 **closed 2026-05-04**: VPS + `openclaw-ops` pin at **2026.5.2** / **`8b2a6e5`**; scripts deploy includes **`openclaw-gws-triage-reply`** literal-escape fix; Telegram **`/triage`** + **`tdr send-now`** UAT passed in-thread. Local CLI + Mac app can be brought to **2026.5.2** on a separate convenience pass.
