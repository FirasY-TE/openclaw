---
status: complete
phase: 02-2-version-alignment-upgrade-stabilization-local-vps-mac-gui
source:
  - 02.2-01-SUMMARY.md
  - 02.2-02-SUMMARY.md
  - 02.2-03-SUMMARY.md
  - 02.2-04-SUMMARY.md
started: "2026-05-04T00:00:00.000Z"
updated: "2026-05-04T12:30:00.000Z"
---

## Current Test

[testing complete]

## Tests

### 1. Cold start after VPS deploy path

expected: After deploy/restart, container healthy; `which openclaw && openclaw -v` shows intended OpenClaw line; primary probe you use still exits 0 when gateway is up.
result: pass

### 2. VPS live version matches ops pin

expected: On VPS `docker exec openclaw-ridl-openclaw-1 sh -lc 'which openclaw && openclaw -v'` prints **OpenClaw 2026.5.2** with short SHA **8b2a6e5** (or the same family as `vps/.openclaw-version` in `openclaw-ops`).
result: pass
validated: "2026-05-04 agent: bare `docker exec sh -lc` resolves `/usr/local/bin/openclaw` (2026.4.21). Production line is `/data/.npm-global/bin/openclaw` → **OpenClaw 2026.5.2 (8b2a6e5)** when `PATH=/data/.npm-global/bin:/usr/local/bin:$PATH` (matches `openclaw-ops/vps/README.md`). Operator earlier confirmed `-v` in Hostinger terminal."

### 3. Health probe from VPS container

expected: `ssh openclaw-vps "docker exec openclaw-ridl-openclaw-1 openclaw channels status --probe"` exits **0** when the gateway is reachable (transient loopback failures right after restart are OK if a retry passes).
result: pass
validated: "2026-05-04 agent: `openclaw channels status --probe` exit 0; Telegram default enabled."

### 4. Phase 02.1 decommission surface stays gone

expected: Container env has no `OPENCLAW_TRIAGE_DRAFT*` variables; `/data/openclaw-gws/bin/openclaw-gws-triage-draft-agent` does not exist.
result: pass
validated: "2026-05-04 agent: no OPENCLAW_TRIAGE_DRAFT in env; draft-agent binary absent."

### 5. Telegram triage digest shape

expected: Running `/triage` (or scheduled digest) in the triage topic shows **inline tokens** per needs-reply item and **no** legacy triage callback draft buttons.
result: pass
validated: "Operator session 2026-05-03/04 — `/triage` digest with inline tokens (e.g. Tesla test); Plan C / no callback buttons."

### 6. Token finalization sends mail

expected: For a disposable test thread, `/bash tdr send-now <token>` sends the Gmail reply; body uses **real line breaks**, not visible literal `\n` sequences (post `openclaw-gws-triage-reply` escape fix).
result: pass
validated: "Operator session — `tdr send-now` sent; `normalize_cli_body_text` deployed via `openclaw-ops` + `deploy.sh` 2026-05-04."

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none — session complete, no issues filed]
