## Plan 03

VPS alignment via `~/openclaw-ops/vps` only (patched tarball lineage per **D-03**); snapshots taken per **D-05** before/after deploy.

## Pre-upgrade VPS

Baseline recorded from operator Mac before this deploy wave:

```bash
ssh openclaw-vps "docker exec openclaw-ridl-openclaw-1 openclaw --version"
```

```
OpenClaw 2026.4.12 (1c0672b)
```

`~/openclaw-ops/vps/.openclaw-version` contained:

```
1c0672b74f66038fd4ee76fbf1c21715887149d8
```

Container: **openclaw-ridl-openclaw-1**.

## Build tarball

```bash
cd ~/openclaw-ops/vps && ./build-patched-openclaw.sh
```

Artifact path (ephemeral Mac temp dir from build):

```
/var/folders/bc/tws9h__516s_tc46kxlqt54c0000gn/T/openclaw-patched-build-54417/openclaw-2026.4.12.tgz
```

```bash
shasum -a 256 /var/folders/bc/tws9h__516s_tc46kxlqt54c0000gn/T/openclaw-patched-build-54417/openclaw-2026.4.12.tgz
```

```
116ce689b1ccd6067d5123a5175047907365fc9049939b04f41e477bfd9d8440  openclaw-2026.4.12.tgz
```

## Deploy

```bash
cd ~/openclaw-ops/vps && OPENCLAW_PACK_TGZ="/var/folders/bc/tws9h__516s_tc46kxlqt54c0000gn/T/openclaw-patched-build-54417/openclaw-2026.4.12.tgz" ./deploy.sh --with-core --restart
```

Deploy completed successfully (`docker compose -p openclaw-ridl restart openclaw`), core pack installed via `npm install -g /tmp/openclaw-core-pack.tgz` inside the container.

## Probe in-container

```bash
ssh openclaw-vps "docker exec openclaw-ridl-openclaw-1 openclaw channels status --probe"
```

Exit code: **0** (CLI reported gateway not reachable on loopback after restart — expected until gateway settles; probe command itself exited zero.)

Post-deploy:

```bash
ssh openclaw-vps "docker exec openclaw-ridl-openclaw-1 openclaw --version"
```

```
OpenClaw 2026.4.12 (1c0672b)
```

## Decommission greps

`rg` is not installed in the container image; equivalent checks:

```bash
ssh openclaw-vps "docker exec openclaw-ridl-openclaw-1 sh -lc 'env | grep OPENCLAW_TRIAGE_DRAFT || echo \"(no OPENCLAW_TRIAGE_DRAFT lines)\"'"
```

Stdout contained `(no OPENCLAW_TRIAGE_DRAFT lines)` — **no** env lines matching `OPENCLAW_TRIAGE_DRAFT`.

```bash
ssh openclaw-vps "docker exec openclaw-ridl-openclaw-1 test ! -f /data/openclaw-gws/bin/openclaw-gws-triage-draft-agent && echo absent"
```

```
absent
```

## Self-Check: PASSED
