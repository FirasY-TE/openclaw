---
phase: 02-2-version-alignment-upgrade-stabilization-local-vps-mac-gui
plan: "04"
subsystem: ops
tags: [macos, openclaw, triage]

requirements-completed:
  - ALN-02.2-MAC
  - ALN-02.2-SNAPSHOT

duration: ""
completed: 2026-05-03
---

## Plan 04

Closure wave: Mac GUI surface (**D-09**, **D-10**), ROADMAP-aligned UAT bookkeeping, project metadata.

## Mac GUI

OpenClaw **Mac app** (`/Applications/OpenClaw.app`) via **Sparkle** channel is **2026.4.11** (`CFBundleVersion` **2026041190**) while local npm and VPS patched core report **2026.4.12** (`1c0672b`). This matches expected **release-family drift** between app bundle and patched-container/npm (**D-09**): not byte-identical packages. Per **D-10**, gateway lifecycle stays on the Mac app (quit/relaunch through **About** / normal app flow per `AGENTS.md` — operator should restart after VPS churn if Telegram UX degrades). Optional follow-up: install newer **Sparkle** build when `2026.4.12` app ships to collapse patch drift.

## UAT

**2026-05-03 — Automated / CLI-verified items:** Local and VPS `openclaw --version` recorded to target pin; `openclaw channels status --probe` exit **0** on Mac CLI and VPS `docker exec`; decommission checks show **no** `OPENCLAW_TRIAGE_DRAFT_*` in container env and **no** `openclaw-gws-triage-draft-agent` binary (`absent`).

**Operator still to verify in Telegram / Gmail live path:** manual triage digest appearance (inline tokens, no legacy callback draft buttons) and `/bash tdr send-now <token>` on a disposable test item — see unchecked rows in `02.2-VERIFICATION.md`.

## Roadmap/state updates

ROADMAP Phase **02.2** **Plans** field lists concrete `02.2-0*-PLAN.md` paths. `STATE.md` bumped for execution progress (plans 01–04 artifacts + deploy transcript).

## Self-Check: PASSED (documentation); human UAT subset pending
