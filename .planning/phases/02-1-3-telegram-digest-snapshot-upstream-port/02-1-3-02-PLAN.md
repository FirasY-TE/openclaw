---
phase: 02-1-3-telegram-digest-snapshot-upstream-port
plan: 02
type: execute
wave: 2
depends_on:
  - 02-1-3-01-PLAN.md
files_modified:
  - vps/.openclaw-version
  - vps/patches/0001-telegram-triage-digest-snapshot.patch
  - vps/build-patched-openclaw.sh
  - vps/deploy.sh
  - vps/README.md
autonomous: true
gap_closure: false
requirements:
  - TRIAGE-02.1.3-5
  - TRIAGE-02.1.3-6
  - TRIAGE-02.1.3-7
must_haves:
  truths:
    - "openclaw-ops reproducibly builds patched openclaw from `vps/.openclaw-version` with digest snapshot symbols present in emitted JavaScript."
    - "`deploy.sh` documents or implements `--with-core` (or named flag) that installs the packed tarball into the container image path used on VPS without ad-hoc single-file copies."
  artifacts:
    - path: vps/build-patched-openclaw.sh
      provides: Idempotent clone-or-fetch, `git checkout $(cat vps/.openclaw-version)`, patch apply, `pnpm install`, `pnpm build`, `npm pack`, echo artifact path.
    - path: vps/patches/0001-telegram-triage-digest-snapshot.patch
      provides: Exact delta produced and checked in Plan 01.
---

<objective>
Make **FirasY-TE/openclaw-ops** the durable source of truth for patched OpenClaw: pin SHA `1c0672b`, add `0001-telegram-triage-digest-snapshot.patch`, implement `vps/build-patched-openclaw.sh`, extend `deploy.sh` for core tarball install, and document the operator loop so VPS runtime actually contains `OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_*` code paths in `node_modules/openclaw/dist`.

**Executor cwd:** `$HOME/openclaw-ops` (set `OPS_ROOT="$HOME/openclaw-ops"` at start of session). All `<automated>` verify commands below assume that path — they will fail if run from the `openclaw/openclaw` repo root.

This plan runs **exclusively** in `~/openclaw-ops/` (or the operator’s clone) — **not** in the `openclaw/openclaw` working tree per `.cursor/rules/vps-ssh-openclaw.mdc`.

</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.cursor/rules/vps-ssh-openclaw.mdc
@.planning/phases/02-1-3-telegram-digest-snapshot-upstream-port/02-1-3-CONTEXT.md
@.planning/phases/02-1-3-telegram-digest-snapshot-upstream-port/02-1-3-RESEARCH.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Pin .openclaw-version to 1c0672b</name>
  <read_first>
    - openclaw-ops/vps/README.md (or create if missing — follow existing repo conventions)
    - openclaw-ops/vps/.openclaw-version (if exists)
  </read_first>
  <files>openclaw-ops/vps/.openclaw-version</files>
  <action>In the **openclaw-ops** clone, set file `vps/.openclaw-version` to **exactly** the 40-character hex `1c0672b` **or** full `1c0672b` short SHA if repo convention uses short form — must match substring `^1c0672b` in first line after edit. Document meaning in README: "Telegram digest snapshot patch base".</action>
  <verify>
    <automated>test -f "$HOME/openclaw-ops/vps/.openclaw-version" && head -1 "$HOME/openclaw-ops/vps/.openclaw-version" | grep -E '^1c0672b'</automated>
  </verify>
  <acceptance_criteria>README.md under `openclaw-ops/vps/` contains literal substring `1c0672b` in the same merge as version file change.</acceptance_criteria>
  <done>Pin recorded.</done>
</task>

<task type="auto">
  <name>Add 0001-telegram-triage-digest-snapshot.patch</name>
  <read_first>
    - /tmp/0001-telegram-triage-digest-snapshot.patch (artifact from Plan 01)
  </read_first>
  <files>openclaw-ops/vps/patches/0001-telegram-triage-digest-snapshot.patch</files>
  <action>Copy Plan 01 generated diff into `vps/patches/0001-telegram-triage-digest-snapshot.patch`. From a **clean** temp clone at `1c0672b`, run `git apply --check` using this file with `-p1` as required. If paths fail, adjust `git diff` generation in Plan 01 (root-relative) — do not hand-edit hunks without re-running tests.</action>
  <verify>
    <automated>grep -q "triage-topic-digest-snapshot" "$HOME/openclaw-ops/vps/patches/0001-telegram-triage-digest-snapshot.patch"</automated>
  </verify>
  <acceptance_criteria>Patch file size is greater than 500 bytes AND contains substring `extensions/telegram/src/bot-message-context.session.ts`.</acceptance_criteria>
  <done>Patch stored in ops repo.</done>
</task>

<task type="auto">
  <name>Implement build-patched-openclaw.sh</name>
  <read_first>
    - openclaw-ops/vps/README.md
    - .cursor/rules/vps-ssh-openclaw.mdc (canonical bash patterns)
  </read_first>
  <files>openclaw-ops/vps/build-patched-openclaw.sh</files>
  <action>Create Bash script executable bit `chmod +x` with bash `set -euo pipefail` that:
1. Reads `SCRIPT_DIR`, resolves `PATCH_DIR="$(dirname …)/patches"` and **VERSION_FILE** path to `.openclaw-version`.
2. `WORK_ROOT="${TMPDIR:-/tmp}/openclaw-patched-build-$$"`; `git clone --depth 1 https://github.com/openclaw/openclaw.git "$WORK_ROOT"` **OR** `git fetch` pattern if maintainers prefer full clone — must end on detached `git checkout <sha>`.
3. Applies `git apply "$PATCH_DIR/0001-telegram-triage-digest-snapshot.patch"` (document `-p` level in comments if not `-p1`).
4. Runs `pnpm install` and `pnpm build` and `npm pack` from repo root; prints final `.tgz` absolute path.
5. Exits non-zero on any failure with stderr message prefix `build-patched-openclaw:`.

Exact commands must mirror maintainer Node/pnpm major version expectations noted in ops README (Node 22+).</action>
<verify>
<automated>bash -n "$HOME/openclaw-ops/vps/build-patched-openclaw.sh"</automated>
</verify>
<acceptance_criteria>Script contains all literal substrings: `set -euo pipefail`, `0001-telegram-triage-digest-snapshot.patch`, `pnpm build`, `npm pack`.</acceptance_criteria>
<done>Builder script present.</done>
</task>

<task type="auto">
  <name>Extend deploy.sh for --with-core</name>
  <read_first>
    - openclaw-ops/vps/deploy.sh (pre-edit)
  </read_first>
  <files>openclaw-ops/vps/deploy.sh</files>
  <action>Add flag `--with-core` (or document chosen name in SUMMARY if existing flag differs) that: copies latest `openclaw-*.tgz` from known build output OR expects `OPENCLAW_PACK_TGZ` env var; runs `npm install -g <tarball>` **inside** `docker exec openclaw-ridl-openclaw-1` **or** via bind-mount instructions exactly matching current operator runbook. Must align with rule: `docker exec` without `-it` for automation. If production path only allows host-side copy into volume, document explicit destination path `/data/.npm-global/lib/node_modules/openclaw/` pattern from ROADMAP success criteria.</action>
  <verify>
    <automated>grep -q "with-core" "$HOME/openclaw-ops/vps/deploy.sh"</automated>
  </verify>
  <acceptance_criteria>`deploy.sh` help text or comment header mentions both `--with-core` and `docker exec` substring.</acceptance_criteria>
  <done>Deploy path scripted.</done>
</task>

<task type="auto">
  <name>VPS grep smoke command + large-digest UAT note</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 02.1.3 success criteria block)
  </read_first>
  <files>openclaw-ops/vps/README.md</files>
  <action>Append subsection **Patched core rebuild** documenting: `./build-patched-openclaw.sh` → `./deploy.sh --with-core --restart` (adjust to real flags); post-install verification commands:

`docker exec openclaw-ridl-openclaw-1 sh -lc 'grep -rl OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_CHAT_ID /data/.npm-global/lib/node_modules/openclaw/dist/*.js | head -5'`

Document large-digest UAT: inflate `triage-digest-latest.md` fixture with sentinel line only in tail past 4096 characters; ask Bella `summary` must mention sentinel.

Escape or quote container name if different — use literal `openclaw-ridl-openclaw-1` as canonical per cursor rule unless README already established another name.</action>
<verify>
<automated>grep -q "OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_CHAT_ID" "$HOME/openclaw-ops/vps/README.md"</automated>
</verify>
<acceptance_criteria>README contains substring `grep -rl OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_CHAT_ID` and substring `4096`.</acceptance_criteria>
<done>Operator runbook closed.</done>
</task>

</tasks>

<verification>
- `bash -n "$HOME/openclaw-ops/vps/build-patched-openclaw.sh"`
- `cd "$HOME/openclaw-ops/vps" && ./build-patched-openclaw.sh` completes on a networked machine — record exit code + artifact name in SUMMARY.
- Dry-run deploy flag parsing: `grep with-core "$HOME/openclaw-ops/vps/deploy.sh"` or `--help` if implemented.
</verification>

<success_criteria>

- Fresh clone + script yields identical tarball hash when rerun (record SHA256 twice in SUMMARY.md).
- Post-deploy VPS grep succeeds (logged in SUMMARY.md with sanitized output snippet).
  </success_criteria>

<output>
After completion create `.planning/phases/02-1-3-telegram-digest-snapshot-upstream-port/02-1-3-02-SUMMARY.md` linking ops commit(s) and VPS verification transcript (redact secrets).
</output>
