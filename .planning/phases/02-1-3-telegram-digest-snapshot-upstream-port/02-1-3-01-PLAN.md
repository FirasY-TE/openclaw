---
phase: 02-1-3-telegram-digest-snapshot-upstream-port
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - extensions/telegram/src/triage-topic-digest-snapshot.ts
  - extensions/telegram/src/bot-message-context.session.ts
  - extensions/telegram/src/triage-topic-digest-snapshot.test.ts
  - extensions/telegram/src/bot-message-context.session.triage-digest-snapshot.test.ts
autonomous: true
gap_closure: false
requirements:
  - TRIAGE-02.1.3-1
  - TRIAGE-02.1.3-2
  - TRIAGE-02.1.3-4
must_haves:
  truths:
    - "At upstream SHA 1c0672b layout, Telegram inbound payload prepends bounded triage digest file content ahead of formatted user Telegram text for matching Forum `(chatId, message_thread_id)` via env OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_CHAT_ID + OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_TOPIC_ID. Per-phase CONTEXT, config-key schema for `triageDigestSnapshot` on `TelegramTopicConfig` (ROADMAP bullet 'Config schema additions') is deferred until upstream JSON Schema allows custom topic keys — env-only MVP ships here."
    - "Missing or whitespace-only snapshot file emits explicit sentinel prose under header '## Canonical triage digest snapshot (not user message)' — never silent drop."
    - "Vitest proves inject-on-env-match and skip-off-match for representative chat/thread IDs."
  artifacts:
    - path: extensions/telegram/src/triage-topic-digest-snapshot.ts
      provides: FS read cap 32768 UTF-16-safe slice per reference implementation semantics; path resolver honoring OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_PATH default `/data/openclaw-gws/output/triage-digest-latest.md`.
    - path: extensions/telegram/src/bot-message-context.session.ts
      provides: Dual injection into envelope-combined Body and BodyForAgent raw prefix chain consistent with Phase 02.1.2 reference.
---

<objective>
Author the Telegram digest snapshot runtime against the **pinned upstream tree** (`1c0672b`): add `extensions/telegram/src/triage-topic-digest-snapshot.ts`, wire `buildTelegramInboundContextPayload`, and land Vitest coverage so the tarball build used on VPS necessarily contains executable snapshot logic.

Produce a **clean `git diff`** from that baseline suitable for exporting as openclaw-ops `0001-telegram-triage-digest-snapshot.patch` (Plan 02 captures file placement — this plan validates behavior + tests).

Output: Passing `pnpm build` + targeted Vitest on a worktree/checkout at **`1c0672b`** (see tasks — do **not** use `git worktree` inside openclaw per AGENTS guidance; use **`/tmp` clone** instead).

</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-1-3-telegram-digest-snapshot-upstream-port/02-1-3-CONTEXT.md
@.planning/phases/02-1-3-telegram-digest-snapshot-upstream-port/02-1-3-RESEARCH.md
@src/telegram/triage-topic-digest-snapshot.ts
@src/telegram/bot-message-context.session.ts
</context>

<tasks>

<task type="auto">
  <name>Baseline clone at 1c0672b and confirm hook file</name>
  <read_first>
    - .planning/phases/02-1-3-telegram-digest-snapshot-upstream-port/02-1-3-RESEARCH.md
  </read_first>
  <files>n/a (temporary directory only)</files>
  <action>On the executor machine run: `git clone https://github.com/openclaw/openclaw.git /tmp/openclaw-patch-02-1-3 && cd /tmp/openclaw-patch-02-1-3 && git checkout 1c0672b`. Verify file exists with: `test -f extensions/telegram/src/bot-message-context.session.ts` exit 0. Record first 25 lines containing import from `openclaw/plugin-sdk` in `02-1-3-01-SUMMARY.md` under "## Baseline".</action>
  <verify>
    <automated>cd /tmp/openclaw-patch-02-1-3 && git rev-parse HEAD | grep -q '^1c0672b'</automated>
  </verify>
  <acceptance_criteria>`02-1-3-01-SUMMARY.md` contains literal substring `1c0672b` inside a fenced or plain line `git rev-parse HEAD` output snippet.</acceptance_criteria>
  <done>Mutable tree at pinned SHA ready.</done>
</task>

<task type="auto">
  <name>Add triage-topic-digest-snapshot.ts (extension layout)</name>
  <read_first>
    - /tmp/openclaw-patch-02-1-3/extensions/telegram/src/bot-message-context.session.ts (first ~120 lines for import style reference)
    - src/telegram/triage-topic-digest-snapshot.ts (semantic reference — read from originating openclaw clone path if /tmp lacks it)
  </read_first>
  <files>/tmp/openclaw-patch-02-1-3/extensions/telegram/src/triage-topic-digest-snapshot.ts</files>
  <action>Create `triage-topic-digest-snapshot.ts` beside other extension sources **in the `/tmp` clone**. Import `TelegramTopicConfig` from `openclaw/plugin-sdk/config-runtime` (matching existing session imports at 1c0672b — adjust if the session file shows a different subpath). Expose verbatim exports: `DEFAULT_TRIAGE_DIGEST_SNAPSHOT_PATH="/data/openclaw-gws/output/triage-digest-latest.md"`; `TRIAGE_DIGEST_SNAPSHOT_MAX_CHARS=32768`; `SNAPSHOT_HEADER` line equals `## Canonical triage digest snapshot (not user message)`; functions `resolveTriageDigestSnapshotPath`, `readTriageDigestSnapshot`, `formatTriageDigestSnapshotSection`, `shouldInjectTriageDigestSnapshot` with env guards `OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_CHAT_ID`, `OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_TOPIC_ID`, `OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_PATH` exactly as spelled in CONTEXT. Implementation note: `shouldInjectTriageDigestSnapshot` returns true when **both** env vars match chat/thread **or** when `topicConfig?.triageDigestSnapshot === true`, but because config schema fields are **not** added in this phase (deferred), the Vitest suite must prove the **env** path; the `topicConfig` branch may remain as dead code until a follow-up adds Zod/types — document that in SUMMARY.</action>
  <verify>
    <automated>grep -q "OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_CHAT_ID" /tmp/openclaw-patch-02-1-3/extensions/telegram/src/triage-topic-digest-snapshot.ts && grep -q "32768" /tmp/openclaw-patch-02-1-3/extensions/telegram/src/triage-topic-digest-snapshot.ts</automated>
  </verify>
  <acceptance_criteria>Typecheck passes locally in clone after file add (`pnpm exec tsc -p extensions/telegram/tsconfig.json` or monorepo equivalent used by upstream at tag — whichever exits 0 in CI).</acceptance_criteria>
  <done>Module compile-clean.</done>
</task>

<task type="auto">
  <name>Wire buildTelegramInboundContextPayload injection</name>
  <read_first>
    - /tmp/openclaw-patch-02-1-3/extensions/telegram/src/bot-message-context.session.ts
    - src/telegram/bot-message-context.session.ts (lines constructing `triageDigestInjection`, `bodyForAgentWithDigest`)
  </read_first>
  <files>/tmp/openclaw-patch-02-1-3/extensions/telegram/src/bot-message-context.session.ts</files>
  <action>Insert import from `./triage-topic-digest-snapshot.js`. Before `finalizeInboundContext` payload assembly where `combinedBody` is built from `body`: compute `snapshotPath` via `resolveTriageDigestSnapshotPath()` with **no** topic-level path argument in this MVP (config field deferred). Run `shouldInjectTriageDigestSnapshot` with `{ isForum, chatId, resolvedThreadId, topicConfig }`; if true, derive `formatTriageDigestSnapshotSection(readTriageDigestSnapshot(snapshotPath))` string and prepend to both Telegram envelope body path and **`BodyForAgent` raw message path** analogous to repo reference (`triageDigestInjection` + `bodyText` coupling). Maintain ordering relative to pending history merge identical to Phase 02.1.2 reference (digest before stitched history).</action>
  <verify>
    <automated>grep -q "formatTriageDigestSnapshotSection" /tmp/openclaw-patch-02-1-3/extensions/telegram/src/bot-message-context.session.ts && grep -q "shouldInjectTriageDigestSnapshot" /tmp/openclaw-patch-02-1-3/extensions/telegram/src/bot-message-context.session.ts</automated>
  </verify>
  <acceptance_criteria>`rg "Canonical triage digest snapshot" /tmp/openclaw-patch-02-1-3/extensions/telegram/src/bot-message-context.session.ts` returns no matches (header lives in helper) **and** helper file contains that exact header string once.</acceptance_criteria>
  <done>Runtime path wired.</done>
</task>

<task type="auto">
  <name>Vitest: snapshot module + session integration</name>
  <read_first>
    - /tmp/openclaw-patch-02-1-3/extensions/telegram/src/bot-message-context.test-harness.ts (or nearest existing session test harness at 1c0672b)
    - src/telegram/triage-topic-digest-snapshot.test.ts
    - src/telegram/bot-message-context.triage-digest-snapshot.test.ts
  </read_first>
  <files>/tmp/openclaw-patch-02-1-3/extensions/telegram/src/triage-topic-digest-snapshot.test.ts, /tmp/openclaw-patch-02-1-3/extensions/telegram/src/bot-message-context.session.triage-digest-snapshot.test.ts</files>
  <action>Port/adapt tests into extension tree using **relative imports** (`./triage-topic-digest-snapshot.js`). Cover: env match enables injection flag; env mismatch disables; missing file sentinel; oversized fixture truncation marker `... (truncated)`; session-level assertion that `BodyForAgent` begins with snapshot header when env set and temp digest file contains unique token `UNIQUE_DIGEST_TOKEN_12345` (use `os.tmpdir()` file path passed via `OPENCLAW_TRIAGE_DIGEST_SNAPSHOT_PATH`). Use `vi.resetModules` or process env restore patterns consistent with neighboring extension tests.</action>
  <verify>
    <automated>cd /tmp/openclaw-patch-02-1-3 && pnpm exec vitest run extensions/telegram/src/triage-topic-digest-snapshot.test.ts extensions/telegram/src/bot-message-context.session.triage-digest-snapshot.test.ts</automated>
  </verify>
  <acceptance_criteria>Command output contains `Test Files  2 passed` **or** single-file equivalent showing **0 failed**; each test file contains substring `UNIQUE_DIGEST_TOKEN_12345` at least once OR documents alternate unique token with same grep pattern in SUMMARY.</acceptance_criteria>
  <done>Automated regression coverage present.</done>
</task>

<task type="auto">
  <name>Build + export diff for ops patch</name>
  <read_first>
    - /tmp/openclaw-patch-02-1-3/package.json (scripts)
  </read_first>
  <files>/tmp/openclaw-patch-02-1-3 (git diff)</files>
  <action>Run `pnpm install` (if needed) and `pnpm build` from clone root; expect exit 0. Run `git status --short` showing only intended paths. Produce `git diff > /tmp/0001-telegram-triage-digest-snapshot.patch` from clone root. Copy patch into operator’s `openclaw-ops` workspace path **manually described in SUMMARY** (absolute path redacted in committed doc — use placeholder `~/openclaw-ops/vps/patches/`). Note: final patch filename assignment is Plan 02 — this task only requires diff artifact bytes exist and `git apply --check` passes from clean `1c0672b` tree.</action>
  <verify>
    <automated>cd /tmp/openclaw-patch-02-1-3 && git apply --check /tmp/0001-telegram-triage-digest-snapshot.patch</automated>
  </verify>
  <acceptance_criteria>`wc -c /tmp/0001-telegram-triage-digest-snapshot.patch` prints a byte count greater than 500 and patch contains literal substring `triage-topic-digest-snapshot`.</acceptance_criteria>
  <done>Patch-ready diff exported.</done>
</task>

</tasks>

<verification>
- `pnpm build` in `/tmp/openclaw-patch-02-1-3`
- `pnpm exec vitest run extensions/telegram/src/triage-topic-digest-snapshot.test.ts extensions/telegram/src/bot-message-context.session.triage-digest-snapshot.test.ts`
</verification>

<success_criteria>

- Digest snapshot logic lives under `extensions/telegram/src/` at baseline `1c0672b`, not only `src/telegram/`.
- Tests fail if injection header or env keys rename/regress.
- Patch check passes for handoff to openclaw-ops.
  </success_criteria>

<output>
After completion create `.planning/phases/02-1-3-telegram-digest-snapshot-upstream-port/02-1-3-01-SUMMARY.md` with commands run, exact vitest lines, and path to generated patch file.
</output>
